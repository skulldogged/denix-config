diff --git a/packages/coding-agent/src/modes/interactive/interactive-mode.ts b/packages/coding-agent/src/modes/interactive/interactive-mode.ts
index a306d90..621279e 100644
--- a/packages/coding-agent/src/modes/interactive/interactive-mode.ts
+++ b/packages/coding-agent/src/modes/interactive/interactive-mode.ts
@@ -763,8 +763,8 @@ export class InteractiveMode {
 	private stopInteractiveTui(): void {
 		if (this.renderer.mode === "fullscreen") {
 			while (this.renderer.hasOverlayEntries) this.renderer.hideOverlay();
-			this.switchTuiMode("regular", false, false);
-			this.renderer.renderNow();
+			this.ui.stop({ preserveScreen: true });
+			return;
 		}
 		this.ui.stop();
 	}
@@ -958,6 +958,7 @@ export class InteractiveMode {
 
 		// Set up theme file watcher
 		onThemeChange(() => {
+			this.ui.setScreenBackground(theme.getScreenBackgroundAnsi());
 			this.ui.invalidate();
 			this.updateEditorBorderColor();
 			this.ui.requestRender();
diff --git a/packages/coding-agent/src/modes/interactive/theme/theme-controller.ts b/packages/coding-agent/src/modes/interactive/theme/theme-controller.ts
index 6d99b0d..ee84138 100644
--- a/packages/coding-agent/src/modes/interactive/theme/theme-controller.ts
+++ b/packages/coding-agent/src/modes/interactive/theme/theme-controller.ts
@@ -9,6 +9,7 @@ import {
 	resolveThemeSetting,
 	setTheme,
 	setThemeInstance,
+	theme,
 	type TerminalTheme,
 	type Theme,
 } from "./theme.ts";
@@ -32,10 +33,12 @@ export class InteractiveThemeController {
 		this.onChanged = onChanged;
 		this.activeThemeName = resolveThemeSetting(this.settingsManager.getThemeSetting(), this.terminalTheme);
 		initTheme(this.activeThemeName, true);
+		this.syncScreenBackground();
 		this.bindTerminalColorSchemeListener();
 	}
 
 	rebindTui(): void {
+		this.syncScreenBackground();
 		this.terminalColorSchemeUnsubscribe?.();
 		this.bindTerminalColorSchemeListener();
 		this.ui.setTerminalColorSchemeNotifications(this.autoSyncEnabled);
@@ -83,6 +86,7 @@ export class InteractiveThemeController {
 		const themeName = resolveThemeSetting(themeSettingOrName, this.terminalTheme) ?? this.activeThemeName;
 		if (!themeName) return;
 		if (setTheme(themeName, true).success) {
+			this.syncScreenBackground();
 			this.ui.invalidate();
 			this.ui.requestRender();
 		}
@@ -107,10 +111,15 @@ export class InteractiveThemeController {
 	}
 
 	private notifyChanged(): void {
+		this.syncScreenBackground();
 		this.ui.invalidate();
 		this.onChanged();
 	}
 
+	private syncScreenBackground(): void {
+		this.ui.setScreenBackground(theme.getScreenBackgroundAnsi());
+	}
+
 	private setAutoSync(enabled: boolean): void {
 		if (this.autoSyncEnabled === enabled) return;
 		this.autoSyncEnabled = enabled;
diff --git a/packages/coding-agent/src/modes/interactive/theme/theme-schema.json b/packages/coding-agent/src/modes/interactive/theme/theme-schema.json
index 039bdc1..b18f199 100644
--- a/packages/coding-agent/src/modes/interactive/theme/theme-schema.json
+++ b/packages/coding-agent/src/modes/interactive/theme/theme-schema.json
@@ -133,6 +133,10 @@
 					"$ref": "#/$defs/colorValue",
 					"description": "Thinking block text color"
 				},
+				"background": {
+					"$ref": "#/$defs/colorValue",
+					"description": "Fullscreen viewport background"
+				},
 				"selectedBg": {
 					"$ref": "#/$defs/colorValue",
 					"description": "Selected item background"
diff --git a/packages/coding-agent/src/modes/interactive/theme/theme.ts b/packages/coding-agent/src/modes/interactive/theme/theme.ts
index b386786..79524ee 100644
--- a/packages/coding-agent/src/modes/interactive/theme/theme.ts
+++ b/packages/coding-agent/src/modes/interactive/theme/theme.ts
@@ -45,7 +45,8 @@ const ThemeJsonSchema = Type.Object({
 		dim: ColorValueSchema,
 		text: ColorValueSchema,
 		thinkingText: ColorValueSchema,
-		// Backgrounds & Content Text (11 required, 1 optional)
+		// Backgrounds & Content Text (11 required, 2 optional)
+		background: Type.Optional(ColorValueSchema),
 		selectedBg: ColorValueSchema,
 		scrollbarThumb: Type.Optional(ColorValueSchema),
 		userMessageBg: ColorValueSchema,
@@ -156,6 +157,7 @@ export type ThemeColor =
 	| "bashMode";
 
 export type ThemeBg =
+	| "background"
 	| "selectedBg"
 	| "scrollbarThumb"
 	| "userMessageBg"
@@ -413,6 +415,10 @@ export class Theme {
 		return ansi;
 	}
 
+	getScreenBackgroundAnsi(): string | undefined {
+		return this.bgColors.get("background");
+	}
+
 	getColorMode(): ColorMode {
 		return this.mode;
 	}
@@ -613,6 +619,7 @@ function createTheme(themeJson: ThemeJson, mode?: ColorMode, sourcePath?: string
 	const fgColors: Record<ThemeColor, string | number> = {} as Record<ThemeColor, string | number>;
 	const bgColors: Record<ThemeBg, string | number> = {} as Record<ThemeBg, string | number>;
 	const bgColorKeys: Set<string> = new Set([
+		"background",
 		"selectedBg",
 		"scrollbarThumb",
 		"userMessageBg",
diff --git a/packages/tui/src/tui-alt-screen.ts b/packages/tui/src/tui-alt-screen.ts
index aa062e4..b2b41f5 100644
--- a/packages/tui/src/tui-alt-screen.ts
+++ b/packages/tui/src/tui-alt-screen.ts
@@ -861,7 +861,7 @@ export class TuiAltScreen extends TuiBase implements ViewportTUI {
 				this.imageProtocol === "kitty" && hadUploadedKittyImages
 					? deleteAllKittyPlacements()
 					: this.deleteKittyImages();
-			buffer += `${clearImages}\x1b[2J`;
+			buffer += `${clearImages}${this.screenBackgroundAnsi ?? ""}\x1b[2J\x1b[49m`;
 		} else if (imagesNeedRedraw) {
 			if (this.imageProtocol === "iterm2") buffer += "\x1b[2J";
 			else if (this.imageProtocol === "kitty") buffer += deleteAllKittyPlacements();
@@ -870,7 +870,12 @@ export class TuiAltScreen extends TuiBase implements ViewportTUI {
 
 		for (let row = 0; row < height; row++) {
 			if (!fullRedraw && !imagesNeedRedraw && screen[row] === this.previousScreen[row]) continue;
-			buffer += `\x1b[${row + 1};1H\x1b[2K${preparedKittyScreen.lines[row] ?? ""}`;
+			const background = this.screenBackgroundAnsi ?? "";
+			const line = preparedKittyScreen.lines[row] ?? "";
+			const paintedLine = background
+				? `${background}${line.replaceAll("\x1b[49m", background).replaceAll("\x1b[0m", `\x1b[0m${background}`)}\x1b[49m`
+				: line;
+			buffer += `\x1b[${row + 1};1H${background}\x1b[2K${paintedLine}`;
 		}
 
 		if (cursorPos) {
diff --git a/packages/tui/src/tui.ts b/packages/tui/src/tui.ts
index 5242fa7..60bd835 100644
--- a/packages/tui/src/tui.ts
+++ b/packages/tui/src/tui.ts
@@ -301,6 +301,7 @@ export interface TUI extends Component {
 	setShowHardwareCursor(enabled: boolean): void;
 	getClearOnShrink(): boolean;
 	setClearOnShrink(enabled: boolean): void;
+	setScreenBackground(ansi?: string): void;
 	setFocus(component: Component | null): void;
 	showOverlay(component: Component, options?: OverlayOptions): OverlayHandle;
 	hideOverlay(): void;
@@ -343,6 +344,7 @@ export abstract class TuiBase extends Container implements TUI {
 	private static readonly MIN_RENDER_INTERVAL_MS = 16;
 	private showHardwareCursor = process.env.PI_HARDWARE_CURSOR === "1";
 	private clearOnShrink = process.env.PI_CLEAR_ON_SHRINK === "1";
+	protected screenBackgroundAnsi: string | undefined;
 	protected fullRedrawCount = 0;
 	protected stopped = false;
 	private pendingOsc11BackgroundReplies = 0;
@@ -411,6 +413,13 @@ export abstract class TuiBase extends Container implements TUI {
 		this.clearOnShrink = enabled;
 	}
 
+	setScreenBackground(ansi?: string): void {
+		if (this.screenBackgroundAnsi === ansi) return;
+		this.screenBackgroundAnsi = ansi;
+		this.resetRenderState();
+		this.requestRender(true);
+	}
+
 	getFocusedComponent(): Component | null {
 		return this.focusedComponent;
 	}
